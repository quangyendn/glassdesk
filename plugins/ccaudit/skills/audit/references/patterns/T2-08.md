---
id: T2-08
name: "Personality instructions banned"
tier: compliance
severity: warn
detection: |
  matches=0
  found=0
  while IFS= read -r f; do
    found=1
    n=$(grep -E -c -i '\b(be|act as|you are) (a |an )?(senior|expert|principal|10x|world-class)\b' "$f" 2>/dev/null); n=${n:-0}
    matches=$((matches + n))
  done < <(_claude_md_files)
  [ "$found" -eq 0 ] && echo "missing" || echo "$matches"
threshold:
  warn_matches: 1
fix_ref: "fixes.md#fix-T2-08"
citation: "anti-pattern A1 — personality framing in CLAUDE.md (claude-md-anti-patterns brainstorm)"
false_positive_risk: medium
---

# T2-08 — Personality instructions banned

## Why it matters
Anthropic best-practices and the anti-patterns study agree: "act as senior/expert" framing is noise. It bloats CLAUDE.md and biases the model toward overconfident drift.

## Detection
Scans every existing CLAUDE.md memory layer (see helper `_claude_md_files`). Any match → warn.

## Target
0 matches.
