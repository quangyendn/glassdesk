---
id: T1-01
name: "CLAUDE.md bloat"
tier: cost
severity: high
cost_share_pct: 14
detection: |
  total=0
  for f in "$HOME/.claude/CLAUDE.md" ".claude/CLAUDE.md"; do
    [ -f "$f" ] || continue
    w=$(wc -w < "$f" | tr -d ' ')
    total=$((total + w))
  done
  echo "$total"
threshold:
  warn_words: 1200
  fail_words: 1500
fix_ref: "fixes.md#fix-T1-01"
citation: "https://code.claude.com/docs/en/best-practices#write-an-effective-claudemd"
false_positive_risk: low
---

# T1-01 — CLAUDE.md bloat

## Why expensive
Loaded every turn, every session. Multiplied by ~200 turns/week.

## Detection
Sum word counts of `~/.claude/CLAUDE.md` + `./.claude/CLAUDE.md`.

## Target
Combined < 1,200 words (~1,500 tokens).
