---
id: T1-04
name: "Cache miss on session resume"
tier: cost
severity: medium
cost_share_pct: 10
detection: |
  echo "manual"
threshold:
  info_manual: true
fix_ref: "fixes.md#fix-T1-04"
citation: "90-day instrumentation study — Pattern 4; default 5-min cache expires during breaks"
false_positive_risk: low
---

# T1-04 — Cache miss on session resume

## Why expensive
System prompt + CLAUDE.md + tools schema re-tokenize at full price (vs 0.1× cache read) after 5-minute default cache expires.

## Detection
Compare first vs subsequent message latency after a break. Emits `manual` — human review required.

## Target
Upgrade to 1-hour cache lifetime on paid plans, or warm cache with a ping if breaks > 4 min.
